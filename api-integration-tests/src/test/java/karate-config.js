function fn() {
  var env = karate.env; // get system property 'karate.env'
  karate.log('karate.env system property was:', env);
  if (!env) {
    env = 'dev';
  }
  var config = {
    env: env,
    veriffBaseUrl: 'https://demo.saas-3.veriff.me/',
    veriffSessionsUrl: 'https://magic.saas-3.veriff.me/api/v2',
  }
  if (env == 'dev') {
    // customize
    // e.g. config.foo = 'bar';
  } else if (env == 'e2e') {
    // customize
  }
  return config;
}